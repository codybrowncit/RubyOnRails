<%= form_for(@recipe) do |f| %>
  <% if @recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% @recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
 <div class="field">
    <%= f.label :picture %><br>
    <%= f.file_field :picture %>
  </div>
  <div class="field">
    <%= f.label :date_added %><br>
    <%= f.date_field :date_added %>
  </div>
  <div class="field">
    <%= f.label :baker %><br>
    <%= f.collection_select :baker_id, Baker.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :gluton_free %><br>
    <%= f.check_box :gluton_free %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
